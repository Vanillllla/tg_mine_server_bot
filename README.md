[![Release](https://img.shields.io/badge/Release-2.4.13-blue)](https://github.com/Vanillllla/tg_mine_server_bot/releases/tag/magma-core)
[![Python](https://img.shields.io/badge/Python-3.13%2B-gree)](https://www.python.org/downloads/)
# 🎮 Minecraft Server Telegram Bot

Управляйте сервером Minecraft прямо из Telegram! Этот бот позволяет запускать, останавливать, перезапускать и настраивать сервер на вашем компьютере удалённо, даже если вас нет дома. Подключайтесь к серверу с друзьями через VPN (Hamachi, Radmin и т.д.).

> ⚠️ **Текущая версия бота доступна только с русскоязычным интерфейсом.** Поддержка английского языка будет добавлена в следующих обновлениях.

## ✨ Основные возможности

*   **Удалённое управление:** Запуск, остановка, перезагрузка сервера через Telegram.
*   **Мониторинг:** Отслеживание состояния сервера, потребления оперативной памяти (RAM) и загрузки процессора (CPU).
*   **Встроенная консоль:** Отправка команд серверу Minecraft прямо в чате с ботом.
*   **Поддержка модов и плагинов:** Используется гибридное ядро **Magma**, поддерживающее моды Forge и плагины Spigot/Bukkit одновременно.
*   **Система регистрации:** Защита паролем для доступа к управлению ботом.
*   **Простая установка модов/плагинов:** Достаточно поместить файлы в соответствующие папки.

## 🚀 Быстрый старт

### Предварительные требования

*   **Python 3.10 или новее**
*   Установленные системные зависимости и модули Python.

### Установка и настройка

1.  **Скачайте последний релиз** [по этой ссылке](https://github.com/Vanillllla/tg_mine_server_bot/releases/tag/magma-core).
2.  **Установите зависимости:**
    ```bash
    pip install asyncio requests aiogram pynvml psutil
    ```
3.  **Настройте переменные окружения:**
    *   Переименуйте файл `.env.example` в `.env`.
    *   Откройте `.env` и заполните обязательные параметры:
        ```env
        # Пароль для регистрации новых пользователей
        REGISTRATION_PASSWORD=ваш_надёжный_пароль
        # Токен вашего бота от @BotFather
        BOT_TOKEN=ваш_токен_бота
        # IP-адрес для подключения друзей (ваш IP в Hamachi/Radmin)
        IP_TODAY=ваш_vpn_ip
        ```
4.  **Запустите бота:**
    ```bash
    python main.py
    ```
    *Держите это окно/терминал открытым для работы бота.*

### Первый запуск сервера

1.  Перейдите в Telegram и найдите своего бота. Нажмите **/start**.
2.  Пройдите регистрацию, введя пароль, указанный в `REGISTRATION_PASSWORD`.
3.  В главном меню нажмите кнопку **"⚙️ Запустить сервер"**.
4.  Бот начнёт процесс запуска. При **первом запуске** сервер остановится, ожидая принятия **EULA**.
5.  В чате с ботом нажмите кнопку **"📟 Режим консоли"** и отправьте команду:
    ```
    yes
    ```
6.  Готово! Сервер запущен. Теперь вы и ваши друзья можете подключиться к нему по адресу `IP_TODAY`.

## ⚙️ Дополнительные настройки

### Установка модов и плагинов
*   **Моды:** Скопируйте `.jar` файлы модов в папку `Server/mods/`.
*   **Плагины:** Скопируйте `.jar` файлы плагинов в папку `Server/plugins/`.

> **Важно:** После добавления **новых модов** рекомендуется удалить старый мир (папку `Server/world/`), чтобы избежать ошибок загрузки структур. При **удалении модов** может потребоваться подтверждение. Для этого в "Режиме консоли" отправьте команду: `fml confirm`.

### Настройка оперативной памяти (RAM)
По умолчанию выделено 12G максимум и 4G минимум. Чтобы изменить:
1.  Откройте файл `SERV_work.py`.
2.  Найдите строку с объявлением класса `ServerManager` и параметрами `xmx` и `xms`:
    ```python
    class ServerManager:
        def __init__(self, jar_file, cwd, xmx="12G", xms="4G", log_dir="logs"):
    ```
3.  Измените значения `xmx` (максимальная память) и `xms` (начальная память) на нужные (например, `"6G"`, `"2048M"`).

### Интерфейс бота
В меню бота доступны кнопки для:
*   Управления сервером (старт, стоп, рестарт).
*   Просмотра статистики (онлайн игроки, использование RAM/CPU).
*   Доступа к консоли сервера.
*   Настройки бота.

## 🛠 Развитие проекта

В планах на будущие обновления:
*   🌐 **Поддержка английского языка.**
*   🔄 **Удобный сменщик ядер сервера.**
*   ⚙️ **Графическая настройка параметров сервера (RAM, автозапуск и др.) через бота.**
*   📦 **Установка модов/плагинов прямо через интерфейс бота.**

**Ваши предложения и идеи приветствуются!** Создавайте [Issue](https://github.com/Vanillllla/tg_mine_server_bot/issues) или пишите в Telegram(те кто его знают :) ).




